import { NextRequest } from 'next/server';import { query } from '@/lib/db';import { verifyAccessToken, extractBearerToken } from '@/lib/auth';import { successResponse, unauthorizedResponse } from '@/lib/api-response';export async function GET(request: NextRequest) {const t = extractBearerToken(request.headers.get('authorization'));if (!t) return unauthorizedResponse();try { verifyAccessToken(t); } catch { return unauthorizedResponse(); }const d = await query('SELECT (SELECT COUNT(*) FROM documents) as docs,(SELECT COUNT(*) FROM users) as users,(SELECT COUNT(*) FROM document_loans WHERE status=\'active\') as loans,(SELECT COUNT(*) FROM requests WHERE status=\'submitted\') as reqs');return successResponse(d.rows[0]);}